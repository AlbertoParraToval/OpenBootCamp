//Notación 

//; -> Sirve para delimitar el final de una linea
const b = "Hola";
b + 4;
//. -> Se utiliza en los objetos para acceder a los atributos
// movil.anchura

//[] -> Se utiliza para las listas, arreglos o arrays.
const ar =[1,2,3,4]
console.log(ar[2]);

//() -> Funciones
function suma(a,b) {
  //Se escribe la funcion
}

//{} -> Llaves para objetos, funciones y estructuras de control.
const movil = {
  anchura :5,
  altura:10
}

if (true) {
  //todo lo que haya entra llaves

  const z = "hola caracola";
}